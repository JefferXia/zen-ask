# OpenRouter API 配置（推荐）
# 从 https://openrouter.ai 获取API密钥
OPENROUTER_API_KEY=your_openrouter_api_key_here

# HTTP Referer（可选，用于OpenRouter统计）
HTTP_REFERER=http://localhost:8000

# 应用名称（可选，用于OpenRouter统计）
APP_NAME=Zen-Ask

# 使用的模型（可选）
# 推荐：deepseek/deepseek-chat-v3（高质量中文模型）
# 免费模型：meta-llama/llama-3.2-3b-instruct:free, microsoft/wizardlm-2-8x22b:free
# 其他模型：anthropic/claude-3-haiku, openai/gpt-4o-mini 等
AI_MODEL=deepseek/deepseek-chat-v3

# 服务端口（可选，默认8000）
PORT=8000

# 主机地址（可选，默认0.0.0.0）
HOST=0.0.0.0

# 运行模式（可选：compatible/lora，默认compatible）
MODE=compatible
